import { Box } from "@mui/material";
import { pageStyle } from "../../styles";

export default function Kontakt({ myRef }) {
  return (
    <Box ref={myRef} id="contact" sx={{ ...pageStyle }}>
      kontalt iund lage
    </Box>
  );
}
